{
  "name": "@pointback/pi",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "keywords": ["pi-package"],
  "pi": { "extensions": ["./dist/index.js"] },
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit" },
  "dependencies": { "@pointback/daemon": "*", "@pointback/protocol": "*", "ws": "^8.18.0" },
  "peerDependencies": { "@earendil-works/pi-coding-agent": "*", "@earendil-works/pi-ai": "*" }
}
